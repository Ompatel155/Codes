#include<stdio.h>

int main(){
int amount;
int a;
    int b;
    char s;
    scanf("%d  %c", &amount, &s);
    switch (s)
    {
    case 'd':
    printf("");
        if (b != 0)
            printf("%f", (float)a / b);
        
        else
            printf("");
    
        break;
    }
}
